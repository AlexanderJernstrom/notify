import React from "react";
import { Typography } from "@material-ui/core";

export default function Description() {
  return (
    <div>
      <Typography color="textPrimary" variant="h3">
        Notify
      </Typography>
      <Typography variant="body1">
        Notify will be the only note app you will ever need.
      </Typography>
      <Typography variant="body1">
        With it's modern design you won't be able to take your look off it.
      </Typography>
      <Typography>
        Notes are saved in the cloud with a click of a button.
      </Typography>
      <Typography>Notify, the future of studying</Typography>
    </div>
  );
}
